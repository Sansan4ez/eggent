# RAG pipeline dependencies.
# Install:  pip install -r scripts/requirements.txt

psycopg2-binary>=2.9      # Postgres driver
openai>=1.0               # OpenAI-compatible embeddings client (also Ollama/LM Studio via base_url)
python-dotenv>=1.0        # .env loading

# Optional — only needed for the corresponding file formats:
pypdf>=4.0                # PDF text extraction  (remove if not vectorizing PDFs)
python-docx>=1.1          # DOCX text extraction (remove if not vectorizing DOCX)
