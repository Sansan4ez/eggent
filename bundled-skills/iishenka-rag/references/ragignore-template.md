# .ragignore — glob patterns (relative to the RAG folder) that rag_sync.py skips.
# One pattern per line. Lines starting with # are comments.
# Machinery (scripts/, .env, index.csv, CLAUDE.md, rag-operator.md, schema.sql,
# requirements.txt, README.md, dotfiles) is always skipped and needs no entry.

# Examples:
# drafts/*
# *.tmp
# archive/**
